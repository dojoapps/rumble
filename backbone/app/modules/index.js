define([
  "app",

  // Libs
  "backbone"
],

function(app, Backbone) {

  // Create a new module
  var Index = app.module();

  // Required, return the module for AMD compliance
  return Index;

});
